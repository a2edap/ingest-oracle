classname: pipelines.cdip.pipeline.CDIPWaveBuoy

triggers:
  - \b[0-9]{3}\b

retriever:
  path: pipelines/cdip/config/retriever.yaml
  overrides:
    /readers/.*/parameters/data_type: historic

dataset:
  path: pipelines/cdip/config/dataset.yaml

quality:
  path: pipelines/cdip/config/quality.yaml
storage:
  path: shared/storage.yaml
